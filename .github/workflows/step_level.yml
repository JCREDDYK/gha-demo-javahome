name: Env at workflo job and step level
on: workflow_dispatch
env:
  APP_ENV: "Dev"
jobs:
  env_demo:
    env:
      APP_ENV: "TEST"
    runs-on: ubuntu-latest
    steps:
      - name: Show Env
        env:
          APP_ENV: "PROD"
      run: echo "App is connecting to $APP_ENV"
  
