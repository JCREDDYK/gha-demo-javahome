name: Multi-shell-cmds

on: workflow_dispatch

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello step
        run: |
         echo "Welcome to GitHub Actions First Project"
         echo "How to rum multiple shell commands in a single step"
